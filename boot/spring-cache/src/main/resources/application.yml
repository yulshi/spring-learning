server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/spring-cache?useSSL=false
    username: root
    password: welcome1
    driver-class-name: com.mysql.jdbc.Driver
  redis:
    host: localhost

mybatis:
  configuration:
    map-underscore-to-camel-case: true

logging:
  level:
    com.example.cache.mapper: debug
